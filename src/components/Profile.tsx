import React from 'react';
import {getKey} from "../utils/local.ts";

const Profile = () => {

    const _id = getKey('_id');

    return (
        <>
            
        </>
    );
};

export default Profile;