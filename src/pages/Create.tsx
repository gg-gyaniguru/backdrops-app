import {Container, CreateDrop} from "../components";

const Create = () => {
    return (
        <>
            <Container className={'my-20'}>
                <CreateDrop/>
            </Container>
        </>
    );
};

export default Create;